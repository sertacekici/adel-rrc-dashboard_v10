rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG: Tüm authentication yapılmış kullanıcılara tam erişim
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
